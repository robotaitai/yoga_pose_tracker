# direnv configuration for automatic venv activation
# If you have direnv installed, this will automatically activate the venv when you cd into this directory
# Install direnv: https://direnv.net/docs/installation.html
# Then run: direnv allow

source_env_if_exists .env
source venv/bin/activate

echo "🧘 Yoga Pose Tracker environment activated automatically via direnv" 